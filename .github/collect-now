# 수동 수집 킥(kick) 스위치
#
# 이 파일을 아무렇게나 수정해서 main에 커밋하면 "회의 수집·요약 파이프라인"이
# 즉시 실행된다(밀린 회의를 바로 반영하고 싶을 때). GitHub Actions의
# "Run workflow" 버튼을 누를 수 없는 환경에서 git push만으로 파이프라인을
# 돌리기 위한 용도다.
#
# 아래 kick 값을 바꿔 커밋하면 된다(값 자체는 의미 없음, 변경 사실만 트리거).
kick: 2026-08-07T-fetch-timeout
